# `@nostosdb/cli` candidate launcher

This unpublished candidate contains one dependency-free JavaScript launcher for the SSPL-1.0 `nostos` CLI. It selects the exact-version native optional package for the current OS and CPU, then delegates without implementing database behavior.

No package in this directory is published by its tests or candidate scripts. See the repository [distribution contract](../DISTRIBUTION.md).
